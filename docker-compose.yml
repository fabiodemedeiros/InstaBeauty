version: '3'

services:
  app:
    build: .
    ports:
      - "5000:5000"
      - "3000:3000"
    volumes:
      - ./:/app
    environment:
      - NODE_ENV=development
      - PORT=5000
      - VITE_PORT=3000
    restart: unless-stopped